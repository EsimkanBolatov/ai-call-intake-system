# 🎯 НАСТРОЙКА ДЛЯ НОМЕРА 87074536449

## 📋 ЧТО НУЖНО ДЛЯ ЗАПУСКА

### 1. СЕРВЕР С ПУБЛИЧНЫМ IP

- **Требуется**: Сервер с публичным IP адресом
- **Варианты**:
  - VPS (DigitalOcean, AWS, Azure, TimeWeb)
  - Выделенный сервер
  - Домашний сервер с пробросом портов

### 2. НАСТРОЙКА SIP TRUNK

- **Требуется**: SIP провайдер для номера 87074536449
- **Варианты**:
  - Локальный оператор связи в Казахстане
  - VoIP провайдер (Twilio, VoIP.ms, и т.д.)
  - Настройка GSM шлюза

### 3. ДОПОЛНИТЕЛЬНЫЕ НАСТРОЙКИ

## 🔧 КОНФИГУРАЦИЯ ДЛЯ ВАШЕГО НОМЕРА

### Файл: `asterisk/config/sip_custom.conf`

```ini
[87074536449]
type=friend
context=incoming
host=dynamic
secret=ваш-пароль
callerid="AI Call Intake" <87074536449>
dtmfmode=rfc2833
canreinvite=no
disallow=all
allow=ulaw
allow=alaw
allow=gsm
qualify=yes
nat=force_rport,comedia
```

### Файл: `asterisk/config/extensions_custom.conf`

```ini
[incoming-custom]
exten => 87074536449,1,NoOp(═► Входящий звонок на 87074536449)
 same => n,Answer()
 same => n,Wait(1)
 same => n,MixMonitor(${UNIQUEID}.wav,ab)
 same => n,Set(LANG=kk)
 same => n,AGI(agi://127.0.0.1/call_handler.py,${CALLERID(num)},${LANG})
 same => n,Hangup()
```

## 🚀 БЫСТРЫЙ ЗАПУСК НА VPS

### Шаг 1: Арендуйте VPS

```bash
# Рекомендуемые параметры:
# - Ubuntu 22.04 LTS
# - 4 ГБ ОЗУ
# - 50 ГБ SSD
# - Публичный IP
```

### Шаг 2: Установите систему

```bash
# Подключитесь к серверу
ssh root@ваш-сервер-ip

# Установите Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh

# Скачайте проект
git clone <ваш-репозиторий>
cd ai-call-intake-system

# Настройте для вашего номера
echo "87074536449" > config/phone_number.txt
```

### Шаг 3: Запустите систему

```bash
# Запустите автоматически
chmod +x run_full_system.sh
./run_full_system.sh start

# Настройте firewall
ufw allow 5060/udp
ufw allow 10000:20000/udp
ufw allow 80/tcp
ufw allow 443/tcp
```

## 📞 НАСТРОЙКА SIP TRUNK ДЛЯ РЕАЛЬНЫХ ЗВОНКОВ

### Вариант A: Использовать существующий номер

1. **Перенаправление вызовов** у вашего оператора на SIP сервер
2. **SIP trunk** от оператора к вашему серверу

### Вариант B: Купить VoIP номер

1. **Купите VoIP номер** у провайдера (например, Twilio)
2. **Настройте SIP trunk** в `sip.conf`:

```ini
[twilio-trunk]
type=peer
host=sip.twilio.com
defaultuser=ваш-account-sid
secret=ваш-auth-token
context=incoming
fromuser=87074536449
```

## 🔐 ЧТО ЕЩЕ НУЖНО

### 1. SSL сертификаты (для HTTPS)

```bash
# Установите certbot
apt install certbot python3-certbot-nginx

# Получите сертификат
certbot --nginx -d ваш-домен.com
```

### 2. Настройка DNS

- A запись: `ai-call.ваш-домен.com` → ваш IP
- SRV запись для SIP (опционально)

### 3. Мониторинг и алертинг

- Настройка Telegram бота для уведомлений
- Настройка email уведомлений
- Мониторинг uptime

### 4. Резервное копирование

```bash
# Автоматические бэкапы
crontab -e
# Добавьте:
0 2 * * * /path/to/ai-call-intake-system/run_full_system.sh backup
```

## 🧪 ТЕСТИРОВАНИЕ ПЕРЕД ЗАПУСКОМ

### Тест 1: Локальный тест

```bash
# На сервере
docker-compose exec asterisk asterisk -rx "sip show peers"
# Должен показать вашу регистрацию

# Тестовый звонок
docker-compose exec asterisk asterisk -rx "channel originate Local/500 extension 500@internal-test"
```

### Тест 2: Внешний тест

```bash
# С другого устройства
# Установите SIP клиент (MicroSIP)
# Настройте:
# - Сервер: ваш-сервер-ip
# - Порт: 5060
# - Логин: 500
# - Пароль: 500
# Позвоните на 500
```

### Тест 3: Реальный звонок

1. Позвоните с любого телефона на 87074536449
2. Система должна ответить: "102 қызметінің автоматты көмекшісісіз"
3. Расскажите о тестовом инциденте
4. Проверьте dashboard: `http://ваш-сервер-ip/`

## 📊 ЧТО БУДЕТ РАБОТАТЬ ПОСЛЕ НАСТРОЙКИ

### Для номера 87074536449:

1. ✅ **Входящие звонки** - Любой может позвонить на этот номер
2. ✅ **Автоответ** - Система ответит на казахском/русском
3. ✅ **Анализ ИИ** - Использует ваш OpenAI API ключ
4. ✅ **Классификация** - Определит тип инцидента
5. ✅ **Логирование** - Сохранит все данные
6. ✅ **Dashboard** - Веб-интерфейс для просмотра

### Пример звонка:

```
[10:00] Звонок на 87074536449
[10:00:02] Система: "102 қызметінің автоматты көмекшісісіз. Қысқаша не болғанын айтыңыз."
[10:00:10] Звонящий: "Менің машинам ұрланды"
[10:00:15] Система: "Қай жерде болды?"
[10:00:20] Звонящий: "Абай көшесі 15"
[10:00:25] Система: "Түсінікті. Деректеріңізді қабылдадық."
```

## 🆘 ЕСЛИ ЧТО-ТО НЕ РАБОТАЕТ

### Проблема: Нет входящих звонков

```bash
# Проверьте порты
netstat -tulpn | grep :5060

# Проверьте firewall
ufw status

# Проверьте SIP регистрацию
docker-compose exec asterisk asterisk -rx "sip show registry"
```

### Проблема: Нет звука

```bash
# Проверьте RTP
docker-compose exec asterisk asterisk -rx "rtp show stats"

# Проверьте кодеки
docker-compose exec asterisk asterisk -rx "core show translation"
```

### Проблема: ИИ не отвечает

```bash
# Проверьте API ключ
docker-compose exec ai-backend env | grep OPENAI_API_KEY

# Проверьте логи
docker-compose logs -f ai-backend
```

## ✅ КРАТКАЯ ИНСТРУКЦИЯ

1. **Арендуйте VPS** с публичным IP
2. **Установите Docker** на сервер
3. **Скачайте проект** и настройте для 87074536449
4. **Запустите систему**: `./run_full_system.sh start`
5. **Настройте SIP trunk** с вашим оператором
6. **Протестируйте**: позвоните на 87074536449
7. **Настройте мониторинг** и бэкапы

## 📞 КОНТАКТЫ ДЛЯ НАСТРОЙКИ

### Для настройки SIP trunk обратитесь:

1. **Ваш мобильный оператор** - запросите SIP trunk услугу
2. **VoIP провайдеры**:
   - Twilio: https://www.twilio.com
   - VoIP.ms: https://voip.ms
   - Local providers in Kazakhstan

### Техническая поддержка:

- **Логи системы**: `docker-compose logs -f`
- **Статус сервисов**: `./run_full_system.sh status`
- **Dashboard**: `http://ваш-сервер-ip/`

**Система готова к настройке для номера 87074536449. После настройки SIP trunk она будет принимать реальные звонки!**
