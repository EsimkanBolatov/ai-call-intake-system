# 🚀 ЗАПУСК ПРОЕКТА СЕЙЧАС!

## ✅ ЧТО У ВАС УЖЕ ЕСТЬ

1. ✅ Zadarma аккаунт
2. ✅ Docker установлен
3. ✅ Проект скачан

## 🖥️ ДЛЯ WINDOWS ПОЛЬЗОВАТЕЛЯ

### Шаг 1: Запустите Docker Desktop

1. Найдите **Docker Desktop** в меню Пуск
2. Запустите его
3. Дождитесь когда иконка в трее станет зеленой (≈1-2 минуты)

### Шаг 2: Запустите систему

**Вариант A: Через командную строку (CMD)**

```cmd
cd C:\Users\Acer\Desktop\ПРОЕКТЫ\Прокуратуара\ai-call-intake-system
start_windows.bat
```

**Вариант B: Двойной клик**

1. Откройте папку проекта
2. Дважды кликните на `start_windows.bat`

### Шаг 3: Проверьте запуск

Откройте браузер и перейдите:

- **Dashboard**: http://localhost:5000
- **Логин**: `admin`
- **Пароль**: `ChangeMe123!`

## 📞 ТЕСТИРОВАНИЕ БЕЗ ZADARMA

### Тест 1: Веб-интерфейс

1. Откройте http://localhost:5000
2. Нажмите "Тестовый звонок"
3. Введите: "Менің машинам ұрланды"
4. Нажмите "Отправить"
5. Посмотрите результат

### Тест 2: Командная строка

```cmd
docker exec ai-call-local-asterisk asterisk -rx "channel originate Local/500@internal-test"
```

### Тест 3: SIP клиент

1. Установите **MicroSIP** (бесплатно)
2. Настройте:
   - Сервер: `localhost`
   - Порт: `5060`
   - Логин: `500`
   - Пароль: `500`
3. Позвоните на номер `500`

## 🔧 НАСТРОЙКА ZADARMA (ПОСЛЕ ЗАПУСКА)

### Шаг 1: Получите SIP данные

1. Войдите в https://my.zadarma.com/
2. Перейдите: **SIP → Мои SIP номера**
3. Нажмите на ваш номер → **Настройки SIP**
4. Скопируйте:
   - **SIP ID** (например: `1234567`)
   - **SIP пароль**

### Шаг 2: Настройте Zadarma

**Используйте Git Bash или WSL:**

```bash
# Откройте Git Bash в папке проекта
./setup_zadarma.sh
```

**Или вручную:**

1. Создайте файл `asterisk/config/zadarma.conf`:

```ini
[zadarma]
type=peer
host=sip.zadarma.com
defaultuser=ВАШ_SIP_ID
secret=ВАШ_SIP_ПАРОЛЬ
context=incoming
fromuser=ВАШ_SIP_ID
qualify=yes
nat=force_rport,comedia
disallow=all
allow=ulaw
allow=alaw
allow=gsm
dtmfmode=rfc2833
```

2. Перезапустите Asterisk:

```cmd
docker restart ai-call-local-asterisk
```

### Шаг 3: Проверьте регистрацию

```cmd
docker exec ai-call-local-asterisk asterisk -rx "sip show registry"
```

**Должно показать:** `Registered`

## 🐛 ЕСЛИ ВОЗНИКЛИ ПРОБЛЕМЫ

### Проблема: Docker не запускается

1. Перезагрузите компьютер
2. Запустите Docker Desktop от имени администратора
3. Проверьте в трее (возле часов) - иконка Docker должна быть зеленой

### Проблема: Порт 5000 занят

```cmd
netstat -ano | findstr :5000
taskkill /PID [PID] /F
```

### Проблема: Контейнеры не запускаются

```cmd
# Проверьте логи
docker logs ai-call-local-asterisk
docker logs ai-call-local-backend

# Пересоберите образы
docker-compose -f docker-compose-local.yml build --no-cache
```

## 📊 ЧТО ДОЛЖНО РАБОТАТЬ

После запуска `start_windows.bat`:

- [ ] http://localhost:5000 открывается
- [ ] Можно залогиниться (admin/ChangeMe123!)
- [ ] Тестовый звонок работает
- [ ] В dashboard появляются звонки
- [ ] AI анализирует речь

## 🎯 БЫСТРЫЙ СТАРТ

```cmd
REM 1. Запустите Docker Desktop
REM 2. Откройте командную строку
cd C:\Users\Acer\Desktop\ПРОЕКТЫ\Прокуратуара\ai-call-intake-system
start_windows.bat
REM 3. Откройте http://localhost:5000
REM 4. Протестируйте!
```

## 🆘 ПОМОЩЬ

### Docker проблемы:

- **Переустановите Docker Desktop**
- **Включите WSL2**: https://docs.microsoft.com/ru-ru/windows/wsl/install

### Zadarma проблемы:

- **Поддержка Zadarma**: support@zadarma.com
- **Чат в личном кабинете**

### AI система:

- **Логи**: `docker logs ai-call-local-backend`
- **Конфигурация**: `production.env` файл

## ✅ ФИНАЛЬНАЯ ПРОВЕРКА

1. ✅ Docker Desktop запущен (зеленая иконка)
2. ✅ `start_windows.bat` выполнен без ошибок
3. ✅ http://localhost:5000 открывается
4. ✅ Можно залогиниться
5. ✅ Тестовый звонок работает
6. ✅ Zadarma настроен (опционально)

**Готово! Система принимает звонки и анализирует их с помощью ИИ.**
