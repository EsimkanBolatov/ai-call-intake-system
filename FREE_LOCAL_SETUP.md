# 🆓 БЕСПЛАТНАЯ ЛОКАЛЬНАЯ УСТАНОВКА

## 🎯 РЕШЕНИЕ БЕЗ СЕРВЕРА И БЕЗ ПЛАТНЫХ УСЛУГ

### Что будет работать БЕСПЛАТНО:

1. ✅ **Локальный Asterisk** на вашем компьютере
2. ✅ **Бесплатные SIP аккаунты** (Zadarma, SIPNET)
3. ✅ **Бесплатный номер** для тестирования
4. ✅ **OpenAI API** с вашим ключом (первые $5 бесплатно)
5. ✅ **Локальный dashboard** на localhost

## 🚀 БЫСТРЫЙ ЗАПУСК НА ВАШЕМ КОМПЬЮТЕРЕ

### Шаг 1: Установите Docker Desktop

- Windows: https://desktop.docker.com/win/main/amd64/Docker%20Desktop%20Installer.exe
- Mac: https://desktop.docker.com/mac/main/amd64/Docker.dmg
- Linux: `sudo apt install docker.io docker-compose`

### Шаг 2: Запустите локальную систему

```bash
# Скачайте проект
git clone <ваш-репозиторий>
cd ai-call-intake-system

# Запустите ЛОКАЛЬНУЮ версию (без внешних портов)
docker-compose -f docker-compose.local.yml up -d
```

## 📞 БЕСПЛАТНЫЙ SIP АККАУНТ

### Вариант 1: Zadarma (бесплатный тестовый номер)

1. Зарегистрируйтесь на https://zadarma.com
2. Получите бесплатный тестовый номер
3. Настройки SIP:
   - Логин: ваш номер
   - Пароль: из личного кабинета
   - Сервер: sip.zadarma.com

### Вариант 2: SIPNET (бесплатный SIP)

1. Зарегистрируйтесь на https://www.sipnet.ru
2. Получите бесплатный SIP аккаунт
3. Можно звонить между SIP аккаунтами бесплатно

### Вариант 3: Локальный SIP (только на компьютере)

```ini
# asterisk/config/sip_local.conf
[600]
type=friend
context=internal
secret=600
host=dynamic
```

## 🔧 КОНФИГУРАЦИЯ ДЛЯ БЕСПЛАТНОГО ИСПОЛЬЗОВАНИЯ

### Файл: `docker-compose.local.yml`

```yaml
version: "3.8"
services:
  asterisk:
    image: andrius/asterisk:18
    container_name: ai-call-local-asterisk
    volumes:
      - ./asterisk/config:/etc/asterisk
      - ./agi:/var/lib/asterisk/agi-bin
    ports:
      - "5060:5060/udp"
      - "10000-20000:10000-20000/udp"
    networks:
      - local-network

  ai-backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    container_name: ai-call-local-backend
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - STT_ENGINE=whisper
      - LLM_ENGINE=openai
      - TTS_ENGINE=coqui
    volumes:
      - ./services:/app/services
      - ./agi:/app/agi
    ports:
      - "8000:8000"
    networks:
      - local-network

  dashboard:
    build:
      context: ./dashboard
      dockerfile: Dockerfile.dashboard
    container_name: ai-call-local-dashboard
    ports:
      - "5000:5000"
    networks:
      - local-network

networks:
  local-network:
    driver: bridge
```

## 📱 КАК ТЕСТИРОВАТЬ БЕЗ РЕАЛЬНОГО НОМЕРА

### Способ 1: Локальный SIP клиент

1. Установите MicroSIP (бесплатно): https://www.microsip.org
2. Настройте:
   - Сервер: `localhost`
   - Порт: `5060`
   - Логин: `600`
   - Пароль: `600`
3. Позвоните на номер `500`

### Способ 2: Эмуляция звонка через скрипт

```bash
# test_local_call.py
import subprocess
import time

# Эмуляция звонка через Asterisk CLI
subprocess.run(['docker', 'exec', 'ai-call-local-asterisk',
                'asterisk', '-rx',
                'channel originate Local/500@internal-test application Playback hello-world'])

time.sleep(5)

# Проверка логов
subprocess.run(['docker', 'logs', '--tail=10', 'ai-call-local-backend'])
```

### Способ 3: Веб-интерфейс для тестирования

1. Откройте `http://localhost:5000`
2. Нажмите "Тестовый звонок"
3. Введите тестовый текст: "Менің машинам ұрланды"
4. Система проанализирует и покажет результат

## 💰 БЕСПЛАТНЫЕ АЛЬТЕРНАТИВЫ OPENAI

### Если закончились бесплатные $5 в OpenAI:

### Альтернатива 1: DeepSeek (бесплатно)

```python
# В services/llm_service.py
LLM_ENGINE = "deepseek"  # Бесплатный API
DEEPSEEK_API_KEY = "ваш-ключ"  # Получите на platform.deepseek.com
```

### Альтернатива 2: Ollama (локально, полностью бесплатно)

```bash
# Установите Ollama
curl -fsSL https://ollama.com/install.sh | sh

# Скачайте модель
ollama pull llama2

# Используйте в системе
LLM_ENGINE = "ollama"
OLLAMA_MODEL = "llama2"
```

### Альтернатива 3: Mock режим (для демо)

```python
# Тестовые ответы без API
LLM_ENGINE = "mock"
# Система будет использовать предопределенные ответы
```

## 🖥 НАСТРОЙКА НА ВАШЕМ КОМПЬЮТЕРЕ

### Windows:

```powershell
# 1. Установите Docker Desktop
# 2. Откройте PowerShell
cd C:\Users\ВашеИмя\Desktop
git clone <репозиторий>
cd ai-call-intake-system

# 3. Запустите
docker-compose -f docker-compose.local.yml up -d

# 4. Откройте браузер
start http://localhost:5000
```

### Mac/Linux:

```bash
# 1. Установите Docker
# 2. Запустите
cd ~/Desktop
git clone <репозиторий>
cd ai-call-intake-system
chmod +x run_local.sh
./run_local.sh
```

## 📊 ЧТО МОЖНО ДЕЛАТЬ БЕСПЛАТНО

### 1. Тестирование системы

- ✅ Звонки между локальными SIP аккаунтами
- ✅ Анализ речи через Whisper (локально)
- ✅ Классификация через OpenAI (первые $5 бесплатно)
- ✅ Dashboard на localhost

### 2. Демонстрация

- ✅ Показать работу системы коллегам
- ✅ Протестировать разные сценарии
- ✅ Настроить под свои нужды

### 3. Разработка

- ✅ Модифицировать код
- ✅ Добавлять новые функции
- ✅ Тестировать на своем компьютере

## 🔄 КАК РАБОТАЕТ БЕСПЛАТНАЯ ВЕРСИЯ

```
Ваш компьютер (localhost)
    ↓
Docker контейнеры:
    - Asterisk (порт 5060)
    - AI Backend (порт 8000)
    - Dashboard (порт 5000)
    ↓
SIP клиент (MicroSIP) → Звонок на 500
    ↓
Asterisk → AGI скрипт → AI анализ
    ↓
Dashboard (localhost:5000) ← Результаты
```

## 🧪 ТЕСТОВЫЕ СЦЕНАРИИ ДЛЯ ЛОКАЛЬНОГО ИСПОЛЬЗОВАНИЯ

### Сценарий 1: Кража автомобиля

```
Звонящий: "Менің машинам ұрланды"
Система: "Қай жерде болды?"
Звонящий: "Абай көшесі 15"
Система: "Түсінікті. Деректеріңізді қабылдадық."
```

### Сценарий 2: Бытовой конфликт

```
Звонящий: "Көршілер айқайласып жатыр"
Система: "Қазір қауіп бар ма?"
Звонящий: "Ия, әйел баласымен қорқып жатыр"
Система: "Түсінікті. Көмек жолдалады."
```

### Сценарий 3: ДТП

```
Звонящий: "Көлік апаты болды"
Система: "Қанша адам?"
Звонящий: "Екі машина, біреу жарақат алды"
Система: "Түсінікті. Жедел көмек жіберіледі."
```

## 🆘 ЕСЛИ ЧТО-ТО НЕ РАБОТАЕТ

### Проблема: Docker не запускается

```bash
# Windows/Mac: Перезапустите Docker Desktop
# Linux: sudo systemctl restart docker
```

### Проблема: Порт 5060 занят

```bash
# Найдите что использует порт
netstat -ano | findstr :5060  # Windows
lsof -i :5060                 # Mac/Linux

# Остановите Skype, Zoom, другие SIP клиенты
```

### Проблема: Нет денег на OpenAI

```bash
# Переключитесь на mock режим
export OPENAI_API_KEY=mock
docker-compose -f docker-compose.local.yml restart ai-backend
```

## ✅ БЫСТРАЯ КОМАНДА ДЛЯ ЗАПУСКА

```bash
# Создайте скрипт run_local.sh
cat > run_local.sh << 'EOF'
#!/bin/bash
echo "Запуск бесплатной локальной версии..."
docker-compose -f docker-compose.local.yml down
docker-compose -f docker-compose.local.yml up -d
echo "Готово!"
echo "Dashboard: http://localhost:5000"
echo "SIP тест: 500@localhost:5060 (пароль: 500)"
EOF

chmod +x run_local.sh
./run_local.sh
```

## 🎯 ЧТО ВЫ ПОЛУЧИТЕ БЕСПЛАТНО

1. **Полнофункциональную систему** на вашем компьютере
2. **Возможность тестировать** все функции
3. **Веб-интерфейс** для управления
4. **Анализ звонков** через ИИ (с вашим API ключом)
5. **Локальную базу данных** с историей звонков
6. **Возможность модификации** кода под свои нужды

**Запустите сейчас на своем компьютере и протестируйте систему БЕСПЛАТНО!**
